#tuple
#similar to the list but not editable
#use round brackets ()

#tuple = (element1, element2,...)

#uses the same functions as the list but not the edit functions
#tuples are faster and safer than lists

#how to convert tuple in list
my_list = list(my_tuple)

#how to convert list in tuple
my_tuple = tuple(my_list) 
